# Zabbix Agent2 Configuration File (Fallback)
# This config is overridden at runtime by the entrypoint
# via container environment variables on the MikroTik.

Server=127.0.0.1
ServerActive=127.0.0.1
Hostname=zabbix-agent2
ListenPort=10050
LogType=console
LogFileSize=0
DebugLevel=3
ControlSocket=/var/run/zabbix/agent2.sock
